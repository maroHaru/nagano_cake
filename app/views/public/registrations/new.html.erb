<div class="container mt-5">
  <div class="row">
    <h4>新規会員登録</h4>
  </div>
  <div class="row">
    <div class="col-md-8 mx-auto">

      <%= form_with model: @customer, url: customer_registration_path do |f| %>
        <%= render "public/shared/error_messages", resource: resource %>
        <table class="table">
          <tr>
            <th>名前</th>
            <td><%= f.label :name, '(姓)' %> <%= f.text_field :last_name %></td>
            <td><%= f.label :name, '(名)' %> <%= f.text_field :first_name %></td>
          </tr>
          <tr>
            <th>フリガナ</th>
            <td><%= f.label :name, '(セイ)' %> <%= f.text_field :last_name_kana %></td>
            <td><%= f.label :name, '(メイ)' %> <%= f.text_field :first_name_kana %></td>
          </tr>
          <tr>
            <th><%= f.label :email, 'メールアドレス' %></th>
            <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
          </tr>
          <tr>
            <th>郵便番号(ハイフンなし)></th>
            <td><%= f.text_field :postal_code %></td>
          </tr>
          <tr>
            <th>住所</th>
            <td><%= f.text_field :address %></td>
          </tr>
          <tr>
            <th>電話番号(ハイフンなし)</th>
            <td><%= f.telephone_field :telephone_number %></td>
          </tr>
          <tr>
            <th>パスワード(6文字以上)</th>
            <td>
              <%= f.label :password %>
              <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %> characters minimum)</em>
              <% end %><br />
              <%= f.password_field :password, autocomplete: "new-password" %>
            </td>
          </tr>
          <tr>
            <th>パスワード(確認用)</th>
            <td>
              <%= f.label :password_confirmation %><br />
              <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
            </td>
          </tr>
          <tr>
            <td><%= f.submit "新規登録", class: "btn btn-md btn-success" %></td>
          </tr>
        </table>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-7 mx-auto">
      <h4>既に登録済みの方</h4>
    </div>
    <div class="col-lg-10 text-center">
      <%= link_to "こちら ", new_customer_session_path %>からログインしてください。
    </div>
  </div>
</div>
